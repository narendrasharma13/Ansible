- name: Print register variable
  hosts: all
  taska:
  - name: store output of shell into register
    ansible.builtin.shell: ls
    register : OUTPUT
    ansible.builtin.debug:
      msg: "print output {{ OUTPUT }}"
